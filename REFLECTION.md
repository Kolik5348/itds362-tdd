1. มีเทสต์ทั้งหมดกี่ตัวและโค้ดจริงกี่บรรทัด อัตราส่วนเป็นเท่าใด
ตอบ เทสต์ทั้งหมด 8 ตัว โค้ดจริงใน kitchen.py ประมาณ 35 บรรทัด คิดเป็นอัตราส่วนประมาณ 1 เทสต์ต่อ 4-5 บรรทัดโค้ด

2. ช่วงที่แถบแดงนานที่สุดคือเมื่อใด อะไรทำให้มันนาน
ตอบ ช่วง A6 (การบวกข้ามหน่วยและการคูณ Sum) ใช้เวลานานที่สุดกว่าจะกลับมาเขียว เพราะต้องคิดเรื่อง deferred computation ก่อน

3. หาจุดที่ใช้ Fake It หนึ่งจุด, Triangulate หนึ่งจุด, Obvious Implementation หนึ่งจุด
ตอบ - Fake It: ตอน A1 (`self.amount = 600`) และตอน A6 ช่วงแรกที่ให้ Converter.reduce คืนค่าตรงๆ ก่อน
- Triangulate: A2 ที่เขียนเทสต์ times(3) กับ times(2) คู่กัน บังคับให้ต้องเขียนสูตรทั่วไปแทนค่าคงที่
- Obvious Implementation: ตอนเขียน CONVERSION_RATES และ reduce สำหรับแปลงหน่วย เขียนโค้ดจริงไปเลยไม่ได้ fake ก่อน 

4. ยังมีรายการใดค้างอยู่ใน test list รายการใดจะทำจริง รายการใดควรลบเพราะเป็นการคาดเดาล่วงหน้า
ตอบ มีโน้ตเรื่อง `__hash__` ที่ค้างไว้ตั้งแต่ A4 ยังไม่มีเทสต์ไหนต้องใช้จริง เลยยังไม่ทำ คิดว่าจะปล่อยไว้ก่อนเพราะเป็นการคาดเดาล่วงหน้า ถ้ามีโจทย์ต้องใช้ค่อยเพิ่ม

5. ดูเมท็อด Quantity.reduce ถ้าออกแบบระบบทั้งหมดบนกระดาษก่อน คุณจะเขียนเมท็อดนี้ออกมาหรือไม่
ตอบ ถ้านั่งออกแบบบนกระดาษก่อน คงไม่ได้คิดถึงแพทเทิร์นนี้ตั้งแต่แรก เพราะการให้ Quantity กับ Sum มีเมธอด reduce แบบเดียวกันแต่ทำงานต่างกัน เป็นสิ่งที่โผล่มาเพราะเทสต์ test_addition_then_multiplication บังคับให้ Sum ต้องรองรับทั้ง reduce และ times เหมือน Quantity ถ้าออกแบบไว้ก่อนน่าจะเขียน Converter ให้เช็ค type แล้วแยกเงื่อนไขแทน ซึ่งจะซับซ้อนกว่านี้